// item = function ( obj ) {

//     this.init = function (obj) {

//         // set this to the current global item id
//         this.id = globalItemIdTracker;
//         // increment the global item id, every item will be unique
//         globalItemIdTracker ++;

//         // Add in properties
//         for ( var i in obj ) {
//             this[i] = obj[i];
//         }
//     }

//     this.init(obj);
// }
// // initialize the global item id tracker int
// globalItemIdTracker = 0;